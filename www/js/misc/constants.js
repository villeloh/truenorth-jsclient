
/**
 * File for app constants that don't seem to fit anywhere else. E.g. the max speed that 
 * the cyclist can go at doesn't conceptually fit in any existing file.
 */

const Constants = {

  MAX_SPEED: 50, // km/h  
  DEFAULT_SPEED: 15, // km/h

}; // Constants